# fsdeploy — dépendances Python
# Installées dans le venv par launch.sh
#
# ── Socle ─────────────────────────────────────────────────────────────────────
configobj>=5.0.8
textual-web>=0.5.0          # inclut textual — couvre terminal ET navigateur
rich>=13.0.0

# ── Système ───────────────────────────────────────────────────────────────────
psutil>=5.9.0
pyudev>=0.24.0
packaging>=23.0
python-magic>=0.4.27        # magic bytes squashfs/cpio/efi (apt: libmagic1)

# ── Config / Sérialisation ────────────────────────────────────────────────────
ruamel.yaml>=0.18.0         # config.yaml ZFSBootMenu — préserve les commentaires

# ── Logging ───────────────────────────────────────────────────────────────────
structlog>=23.0.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
humanize>=4.0.0
watchfiles>=0.21.0
typer>=0.12.0
aiofiles>=23.0.0            # I/O fichiers async — cohérence code Textual

# ── Compression ───────────────────────────────────────────────────────────────
zstandard>=0.22.0           # export/import snapshots .zst

# ── Stream ────────────────────────────────────────────────────────────────────
python-ffmpeg>=2.0.0

# ── Phase initramfs/réseau ────────────────────────────────────────────────────
pyroute2>=0.7.0             # config réseau netlink — mode stream initramfs
